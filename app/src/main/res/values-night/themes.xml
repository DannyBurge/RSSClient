<resources>
    <!-- Base application theme. -->
    <style name="RSSClient" parent="AppTheme">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/allTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/material_purple_300</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
    <!-- Customize your theme here. -->


    <color name="bottomBarMenuColor">@color/material_gray_700</color>
    <color name="cardBackground">@color/semi_clear_white</color>
    <color name="cardPicBackground">@color/semi_clear_white</color>
    <color name="allTextColor">@color/myGray</color>

    <color name="backgroundTempBar">@color/semi_clear_white</color>

    <color name="main_bg_color">@color/material_gray_700</color>
</resources>